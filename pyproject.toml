[tool.poetry]
name = "bot"
version = "0.1.0"
description = "Discord bot for communities"
authors = ["mouahahaaa"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
nextcord = "^3.1.0"
python-dotenv = "^1.1.1"
pynacl = "^1.5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
